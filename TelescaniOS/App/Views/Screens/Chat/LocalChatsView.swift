import SwiftUI

struct LocalChatsView: View {
    
    var body: some View {
        VStack {
            Text("Local Chats")
                .foregroundColor(.gray)
                .padding()
        }
    }
}

