import SwiftUI

import SwiftUI

struct NearView: View {
    var body: some View {
        VStack {
            Text("People nearby content")
                .frame(maxWidth: .infinity, maxHeight: .infinity)
        }
    }
}
